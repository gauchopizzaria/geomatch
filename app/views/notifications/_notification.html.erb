<% actor = notification.actor %>
<% is_locked = !current_user.can_see_who_liked_me? %>

<%# Adiciona classe 'locked' se for Free %>
<div class="like-card <%= 'locked' if is_locked %>" data-notification-id="<%= notification.id %>">
  
  <%# SE BLOQUEADO: Link abre o Modal de Upgrade %>
  <%# SE LIBERADO: Link abre o Perfil do Usuário %>
  <% link_target = is_locked ? plans_path : user_path(actor) %>
  
  <%# Se bloqueado, adicionamos um data-controller para abrir o modal via JS (opcional) ou link direto %>
  <%# Aqui vou usar um link normal para Plans ou Perfil, mas interceptaremos com JS se quiser modal %>
  
  <%= link_to link_target, class: "like-card-link #{'trigger-upgrade-modal' if is_locked}" do %>
    
    <%# Imagem de Fundo %>
    <% if actor.avatar.attached? %>
      <%= image_tag actor.avatar, class: "like-card-img" %>
    <% else %>
      <%= image_tag "default-avatar.png", class: "like-card-img" %>
    <% end %>

    <%# --- CAMADA DE CADEADO (Só aparece se bloqueado) --- %>
    <% if is_locked %>
      <div class="locked-overlay">
        <div class="locked-icon">🔒</div>
        <div class="locked-text">Ver Perfil</div>
      </div>
    <% end %>

    <%# --- INFORMAÇÕES (Escondemos detalhes se bloqueado) --- %>
    <% unless is_locked %>
      <div class="like-card-gradient"></div>

      <div class="like-card-info">
        <div class="location-badge-small">
          <svg width="10" height="10" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
          Perto de mim
        </div>

        <div class="user-details">
          <span class="name"><%= actor.display_name.split.first %></span>
          <span class="age"><%= actor.age rescue "" %></span>
          <% if actor.respond_to?(:verified) && actor.verified %>
             <%= image_tag "verificado.svg", class: "verified-icon-small" rescue nil %>
          <% end %>
        </div>
      </div>
    <% end %>

  <% end %>
</div>